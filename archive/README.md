# Archive directory

This folder contains legacy/developer-only materials that are kept for transparency
but are not part of the recommended reviewer workflow.

- `dev_old/legacy_scripts/`: deprecated script variants moved out of `scripts/`
- `release_bundle/legacy_runtime_logs/`: historical runtime logs from prior executions

Reviewers should ignore this directory unless they need historical context.
