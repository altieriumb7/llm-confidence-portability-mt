# Archived paper notes

These notes are preserved for project history and debugging context.

They are not authoritative for the reviewer-facing reproducibility flow.
Use the top-level `README.md` and `scripts/reproduce_offline_artifact.sh` instead.
